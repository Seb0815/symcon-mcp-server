{
  "name": "symcon-mcp-server",
  "version": "2.0.0",
  "description": "MCP Server for IP-Symcon - Docker-based, exposes Symcon API as MCP tools",
  "author": "Niko Sinthern",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "docker:build": "npm run build && docker build -t symcon-mcp-server:latest -f Dockerfile .",
    "docker:start": "docker-compose up -d",
    "docker:stop": "docker-compose down",
    "docker:logs": "docker logs -f symcon-mcp-server"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
